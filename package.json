{
  "name": "quiz",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "npm run build; gh-pages -d dist",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.0.1"
  }
}
